# PRD — 취약 단어 타겟형 토익 문제 생성기 (v1.0)

| 항목 | 내용 |
|---|---|
| 제품명 | 취약 단어 타겟형 토익 문제 생성기 |
| 문서 버전 | v1.0 (기획서 v0.2 + 기본 기획서 + 예외처리 리스트 통합) |
| 범위 | **단일 화면 · 핵심 기능 1개 · 로그인/결제/DB 없음** |
| 개발 예산 | 5시간 (1인) |
| 기술 스택 | **본 문서에서 지정하지 않음.** "무엇을 만들지"만 규정하며, 구현 언어·프레임워크·AI 모델 선택은 구현자 재량 |

---

## 0. 선행 정리 — 입력 문서 간 충돌 해소

두 기획서 사이에 어긋나는 지점이 있어, 구현자가 헷갈리지 않도록 먼저 확정합니다. **아래 확정값이 본 PRD 전체에 적용됩니다.**

| 쟁점 | 기본 기획서 | 기획서 v0.2 (사용자 수정 반영본) | **본 PRD 확정** | 근거 |
|---|---|---|---|---|
| 파트 선택 | "파트, 난이도를 선택" | 파트 선택 UI 제거, Part 5 고정 | **Part 5 고정. 선택 UI 없음** | 사용자가 v0.2에서 명시적으로 수정 지시 |
| 문항 수 | 명시 없음 (초기 5문항) | 3문항 | **3문항** | 사용자 수정 지시 |
| 문제 유형 | 명시 없음 | 어휘 / 어법 / 전치사·접속사 혼합 | **3유형 혼합** | 사용자 수정 지시 |
| SNS 공유 | 없음 | 추가 (텍스트 공유 한정) | **포함하되 보조 기능**. 핵심 기능 1개(문제 생성·풀이) 판정에 영향 없음 | 사용자 추가 지시 + "기능 1개" 제약 양립 |
| AI 제공자 | "Gemini API" 명시 | 명시 없음 | **명시하지 않음.** "AI 생성 엔진 1회 호출"로만 규정 | 본 요청의 "기술 스택 지정 금지" 제약이 우선 |
| 응답 목표 시간 | 5초 | P50 5초 / P90 10초 | **P50 5초 / P90 10초, 하드 타임아웃 20초** | 5초는 중앙값 목표로만 성립 가능. 상세 §1.2 |

> **단일 화면의 정의:** 페이지 이동·라우팅 없이 **하나의 화면 안에서 상태(state)만 전환**됩니다. 입력 → 풀이 → 결과는 별개 페이지가 아니라 같은 화면의 3가지 표시 상태입니다.

---

## 1) 목표와 성공조건

### 1.1 목표

**해결할 문제.** 토익 수험생은 단어와 뜻만 반복 암기하고 실제 문제에 적용해보지 않아 금세 잊습니다. 자신이 약한 단어는 이미 알고 있지만(오답노트·단어장), **그 특정 단어를 물어보는 문제를 구할 방법이 없습니다.**

**제품이 하는 일.** 취약 단어를 입력하면 **AI 1회 호출**로 Part 5 문제 3문항과 정답, 그리고 모든 선지의 해설을 **한 번에** 생성합니다. 이후 사용자가 답을 고르는 순간 **추가 호출 없이 즉시** 해설이 열립니다.

**핵심 설계 원칙 (구현 시 절대 위반 금지).**
1. **AI 호출은 세션당 정확히 1회.** 답안 선택·해설 표시·결과 요약 어디에서도 추가 호출이 발생해서는 안 됩니다.
2. **생성 결과는 전량 클라이언트 메모리에 보관.** 해설을 나중에 가져오는 지연 로딩 방식은 금지입니다.
3. **어떤 데이터도 영속 저장하지 않음.** 서버 DB는 물론 브라우저 저장소(localStorage / sessionStorage / 쿠키 / IndexedDB)도 사용하지 않습니다. 새로고침 시 전부 초기화되는 것이 정상 동작입니다.

### 1.2 성공조건

| ID | 지표 | 목표값 | 측정 방법 |
|---|---|---|---|
| S1 | 문제 생성 속도 | 생성 버튼 클릭 → 첫 문항 표시까지 **중앙값 5초 이내, 90퍼센타일 10초 이내** | 20회 연속 생성, 클릭~렌더 시각 기록 |
| S2 | 해설 표시 속도 | 선지 선택 → 해설 렌더 **200ms 이내**, 그 사이 **네트워크 요청 0건** | 브라우저 네트워크 탭 실측 |
| S3 | 응답 파싱 성공률 | **95% 이상** (자동 재시도 1회 포함) | 20회 연속 생성 |
| S4 | 타깃 단어 반영률 | 생성된 문항의 **90% 이상**에서 입력 단어가 지문 또는 선지에 실제 등장 | 20회 × 3문항 = 60문항 수기 확인 |
| S5 | 정답 유일성 | 정답이 유일하게 성립하는 문항 **90% 이상** | 동일 60문항 수기 확인 |
| S6 | 유형 혼합률 | 3문항의 유형이 **전부 동일한 경우 0%** (최소 2유형 보장) | 20회 생성 시 유형 태그 집계 |
| S7 | 학습 가치 | 사용자 5명 중 **4명 이상**이 "내가 고른 오답이 왜 틀렸는지 알게 됐다"에 동의 | 대면 인터뷰 |
| S8 | 완주율 | 테스트 세션의 **70% 이상**이 3문항 전부 풀이 | 세션 관찰 |

> **S1에 대한 주의:** 기본 기획서의 "5초 안에"는 **중앙값 기준**으로만 달성 가능한 수치입니다. AI 생성 지연은 분포가 넓어 모든 요청을 5초 안에 끝내는 것을 보장할 수 없습니다. 따라서 5초는 P50 목표로 두고, P90 10초 / 하드 타임아웃 20초를 함께 정의합니다. 구현자는 5초를 넘겼다고 요청을 중단해서는 안 됩니다.

---

## 2) 사용자 스토리

**타깃 사용자.** 토익 600~800점대, 시험을 3주~3개월 내 앞둔 수험생. 오답노트나 단어 리스트를 이미 보유. 하루 10~20분 짬 학습. 문법 용어는 알지만 문장 안에서 구분이 안 되는 상태.

| ID | 사용자 스토리 | 인수 조건 (Acceptance Criteria) |
|---|---|---|
| US-1 | 수험생으로서, **내가 자꾸 틀리는 단어를 직접 입력**하고 싶다. 그 단어가 나오는 문제를 풀어야 체화되기 때문이다. | 단어 1~5개를 쉼표로 구분해 입력할 수 있다. 입력한 단어가 생성된 문항에 실제로 등장한다. |
| US-2 | 수험생으로서, **내 수준에 맞는 난이도**를 고르고 싶다. 너무 쉽거나 어려우면 시간 낭비이기 때문이다. | 쉬움/보통/어려움 중 하나를 선택할 수 있고, 선택값이 생성 결과에 반영된다. |
| US-3 | 수험생으로서, **버튼 한 번으로 문제가 다 만들어지길** 원한다. 중간에 기다리는 구간이 여러 번이면 흐름이 끊기기 때문이다. | 생성 버튼 클릭 1회로 3문항 전체가 준비된다. 이후 풀이 중 로딩이 다시 나타나지 않는다. |
| US-4 | 수험생으로서, **어휘 뜻만 묻는 문제가 아니라 어법·전치사/접속사 문제도 섞여** 나오길 원한다. 어휘만 나오면 뜻만 보고 찍는 습관이 들어 실전에 도움이 안 되기 때문이다. | 3문항 중 최소 2가지 유형이 포함되며, 각 문항에 유형이 표시된다. |
| US-5 | 수험생으로서, 답을 고른 **즉시 해설을 보고** 싶다. 기다리는 동안 집중이 끊기기 때문이다. | 선지 클릭 후 200ms 이내에 해설이 표시된다. |
| US-6 | 수험생으로서, **정답 근거뿐 아니라 내가 고른 오답이 왜 틀렸는지** 알고 싶다. 그래야 같은 실수를 반복하지 않기 때문이다. | 선택한 선지가 오답이면 그 선지에 대한 전용 해설이 표시된다. 4개 선지 각각의 해설이 모두 준비되어 있다. |
| US-7 | 수험생으로서, **문장 전체의 한글 해석**을 보고 싶다. 문장을 통째로 이해해야 단어가 기억에 남기 때문이다. | 해설과 함께 지문의 한글 해석이 표시된다. |
| US-8 | 수험생으로서, **결과를 복사하거나 공유**하고 싶다. 저장 기능이 없으니 스터디방이나 내 메모앱에 남겨야 하기 때문이다. | 결과 상태에서 전체 복사 버튼과 공유 버튼이 동작한다. |
| US-9 | 수험생으로서, **다른 단어로 바로 다시** 시작하고 싶다. 여러 단어를 연속으로 훈련하기 때문이다. | 새로 시작 버튼으로 입력 상태로 돌아가며, 이전 결과는 초기화된다. |

---

## 3) 화면 명세

**화면은 1개**입니다. 아래 3개는 같은 화면의 **표시 상태**이며, 페이지 이동이 아닙니다.

```
┌─────────────────────────────────────────────┐
│  [상태 A] 입력                               │
│  ─────────────────────────────              │
│  제목 / 한 줄 설명                           │
│  취약 단어 입력창 (쉼표 구분, 1~5개)          │
│  난이도 선택: 쉬움 / 보통 / 어려움            │
│  [문제 만들기] 버튼                          │
│  안내: "결과는 저장되지 않습니다"             │
└─────────────────────────────────────────────┘
                  │ 생성 버튼 → AI 1회 호출
                  ▼
┌─────────────────────────────────────────────┐
│  [상태 A-1] 로딩 (A 위에 겹쳐 표시)           │
│  스피너 + "3문항을 한 번에 만드는 중"         │
│  ※ 타이핑 스트리밍 연출 금지                  │
└─────────────────────────────────────────────┘
                  │ 파싱·검증 성공
                  ▼
┌─────────────────────────────────────────────┐
│  [상태 B] 풀이  ← 네트워크 호출 0건           │
│  진행 표시: 1 / 3        유형 배지: [어법]    │
│  지문 (빈칸 포함)                            │
│  ○ A  ○ B  ○ C  ○ D                        │
│  ── 선지 클릭 시 즉시 아래 펼침 ──            │
│   ✓/✗ 정오 표시                             │
│   정답 근거 해설                             │
│   내가 고른 오답이 틀린 이유                  │
│   한글 해석                                  │
│   타깃 단어 한 줄 정리                        │
│  [다음 문제] (마지막 문항이면 [결과 보기])     │
└─────────────────────────────────────────────┘
                  │ 3문항 완료
                  ▼
┌─────────────────────────────────────────────┐
│  [상태 C] 결과                               │
│  n / 3 정답                                  │
│  유형별 정오표: 어휘 O · 어법 X · 전접부 O    │
│  틀린 문항의 타깃 단어 목록                   │
│  [결과 전체 복사] [공유] [새로 시작]          │
│  안내: "이 결과는 저장되지 않습니다"          │
└─────────────────────────────────────────────┘
```

### 3.1 상태 A — 입력

| 요소 | 명세 |
|---|---|
| 단어 입력창 | 멀티라인 아님. 쉼표 구분. **1~5개**. 각 단어는 영문자·하이픈·공백만 허용(§5 EX-3 참조). placeholder 예: `comprehensive, comprehensible` |
| 난이도 선택 | 3지 택1(쉬움/보통/어려움). **기본 선택값 없음** — 미선택 상태로 시작하며 사용자가 반드시 하나를 골라야 함(§5 EX-2 참조) |
| 파트 선택 | **UI 없음.** Part 5로 내부 고정 |
| 생성 버튼 | 단어·난이도 조건을 모두 만족하기 전까지 **비활성**. 비활성 이유를 인라인 안내 문구로 표시 |
| 저장 안내 | "결과는 저장되지 않습니다. 새로고침 시 사라집니다." 상시 노출 |

### 3.2 상태 B — 풀이

| 요소 | 명세 |
|---|---|
| 진행 표시 | `현재 / 전체` 형식. 전체 수는 **실제 수신·검증 통과한 문항 수**를 따름(3이 아닐 수 있음, §5 EX-4) |
| 유형 배지 | `어휘` / `어법` / `전치사·접속사` 중 하나를 문항 상단에 표시 |
| 선지 | 항상 4개. 선택 전에는 정답이 노출되지 않아야 함 |
| 선택 동작 | **1회만 선택 가능.** 선택 후 다른 선지로 변경 불가(정답 확인 후 바꿔 고르는 것 방지) |
| 해설 영역 | 선택 즉시 펼침. 4개 블록(정답 근거 / 내 오답 이유 / 한글 해석 / 단어 정리). 정답을 맞힌 경우 "내 오답 이유" 블록은 숨김 |
| 이동 | 이전 문항으로 돌아가기 **없음** (단방향). 마지막 문항에서 버튼 라벨이 `결과 보기`로 바뀜 |

### 3.3 상태 C — 결과

| 요소 | 명세 |
|---|---|
| 점수 | `n / 전체문항수` |
| 유형별 정오표 | 출제된 유형만 표시. 미출제 유형은 표시하지 않음 |
| 복사 버튼 | 결과 요약 텍스트를 클립보드에 복사 + 완료 토스트 |
| 공유 버튼 | 시스템 공유 시트 호출. 미지원 환경이면 **자동으로 클립보드 복사로 대체**(§5 EX-10). 공유 대상은 **텍스트만** — 이미지 카드 생성·리치 링크 없음 |
| 공유 텍스트 템플릿 | `취약 단어 [단어들]로 토익 문제 3개 풀었어요! 정답 n/3 (어휘 O · 어법 X · 전치사·접속사 O) — 나도 풀어보기: [서비스 URL]` |
| 새로 시작 | 모든 상태를 초기화하고 상태 A로 복귀 |

---

## 4) 기능 명세

### 4.1 핵심 기능 (1개) — 취약 단어 기반 문제 생성 및 즉시 해설 풀이

#### F-1. 입력 수집
- 단어 문자열을 쉼표로 분리 → 앞뒤 공백 제거 → 빈 항목 제거 → **중복 제거** → 최대 5개까지 인정
- 난이도 값 1개 수집
- 파트는 사용자 입력 없이 `Part 5`로 고정 전달

#### F-2. AI 1회 호출
- **세션당 정확히 1회.** 3문항 전체를 하나의 응답으로 요청
- 응답은 **구조화된 데이터(JSON)만** 반환하도록 지시. 설명문·마크다운 코드펜스가 섞여 오는 경우를 전제하고, 파싱 전 코드펜스 제거 전처리를 반드시 수행

#### F-3. 문제 유형 배분 규칙 (핵심 요구사항)

| 유형 코드 | 이름 | 무엇을 묻는가 |
|---|---|---|
| `vocab` | 어휘 | 타깃 단어와 혼동되는 유의어·파생어 중 문맥에 맞는 것 고르기 |
| `grammar` | 어법 | 타깃 단어(또는 파생어)의 품사·시제·수일치·태 등 형태 고르기 |
| `prep_conj` | 전치사·접속사 | 타깃 단어와 호응하는 전치사, 또는 문장 연결 접속사 고르기 |

**배분 규칙:**
- 3문항 중 **`vocab` 최소 1문항 필수**
- **`grammar` 또는 `prep_conj` 중 최소 1문항 필수**
- 나머지 1문항은 입력 단어 특성에 맞게 자유 배정
- **3문항의 유형이 전부 동일한 것은 금지** — 생성 지시문에 명시하고, 수신 후 클라이언트에서도 검증(§5 EX-6)
- 입력 단어가 1개뿐이어도 세 유형 모두 구성 가능(어휘=단어 자체 / 어법=활용형 / 전치사·접속사=호응 연결어)

#### F-4. 응답 데이터 요구사항

각 문항이 **반드시** 포함해야 하는 필드:

| 필드 | 설명 | 누락 시 |
|---|---|---|
| `id` | 문항 번호 | 인덱스로 대체 |
| `type` | `vocab` / `grammar` / `prep_conj` | 배지 미표시로 진행 |
| `targetWord` | 이 문항이 겨냥한 입력 단어 | 검증 불가 → 문항 폐기(EX-5) |
| `stem` | 빈칸 포함 영어 지문 | 문항 폐기 |
| `choices` | 선지 **정확히 4개** (key + text) | 문항 폐기(EX-7) |
| `answer` | 정답 key. `choices`의 key 중 하나여야 함 | 문항 폐기 |
| `explanations` | **4개 선지 각각의 해설** (정답 근거 1 + 오답 이유 3) | 문항 폐기(EX-8) |
| `translation` | 지문의 한글 해석 | 해당 블록만 숨기고 진행 |
| `wordNote` | 타깃 단어 한 줄 정리 | 해당 블록만 숨기고 진행 |

> **필수 vs 선택의 기준:** 그것이 없으면 **문제를 풀 수 없거나 학습 가치가 0**이 되는 필드만 "문항 폐기" 대상입니다. `translation`·`wordNote`는 없어도 문제 풀이가 성립하므로 부분 표시로 진행합니다.

#### F-5. 즉시 해설 표시
- 수신한 3문항 전체를 메모리에 보관
- 선지 클릭 시 **메모리에서 즉시 조회**하여 렌더. 네트워크 접근 금지
- 정답 시: 정답 근거 + 해석 + 단어 정리
- 오답 시: 정오 표시 + 정답 근거 + **내가 고른 선지의 오답 이유** + 해석 + 단어 정리

#### F-6. 결과 집계 및 공유
- 정답 수, 유형별 정오, 틀린 문항의 타깃 단어 집계
- 복사 / 공유 / 새로 시작

### 4.2 명시적 비기능 (구현하지 말 것)

| # | 만들지 않을 것 | 구체 범위 |
|---|---|---|
| 1 | 계정 | 회원가입·로그인·소셜로그인·비밀번호 재설정·게스트 ID 발급. **버튼조차 만들지 않음** |
| 2 | 모든 저장 | 서버 DB, localStorage, sessionStorage, 쿠키, IndexedDB, 파일 다운로드. 학습 이력·오답노트·지난 세션 불러오기 없음 |
| 3 | 결제 | 구독·결제창·무료 횟수 제한·사용량 카운터·요금 페이지 |
| 4 | 파일 입력 | PDF/CSV/TXT 업로드, OCR, 이미지 단어 추출, 문단에서 단어 자동 선별 |
| 5 | Part 5 외 전 파트 | Part 1~4(오디오), Part 6, Part 7. **파트 선택 UI 자체를 만들지 않음** |
| 6 | 적응형 학습 | 정답률 기반 난이도 자동 조정, 오답 단어 자동 재출제, 복습 주기 |
| 7 | 음성 | TTS 발음 재생, 발음 기호, 녹음·쉐도잉 |
| 8 | 스트리밍 출력 | 응답을 한 글자씩 흘리는 타이핑 연출 (첫 문항 표시를 늦추므로 금지) |
| 9 | 재생성 | 문항별 다시 만들기, 비슷한 문제 더 보기, 세션 중 추가 호출 |
| 10 | 백오피스 | 문항 신고, 관리자 대시보드, 검수 큐, 분석 도구 연동 |
| 11 | 공유 고도화 | 결과 카드 이미지 생성, 메신저 SDK 리치 링크, 공유 클릭 통계 |
| 12 | 부가 UI | 다국어, 다크모드 토글, 온보딩 튜토리얼, 화면 전환 애니메이션 |

---

## 5) 예외 처리 ★ (본 문서의 핵심 섹션)

### 5.0 예외 처리 원칙

1. **사용자에게 기술 용어를 노출하지 않는다.** "JSON 파싱 실패", "HTTP 500" 같은 문구를 화면에 쓰지 않습니다.
2. **모든 막다른 길에는 복귀 버튼이 있다.** 어떤 오류 화면에서도 사용자가 다음 행동을 할 수 있어야 합니다.
3. **부분 성공은 살린다.** 3문항 중 2문항만 정상이면 2문항으로 진행합니다. 전부 버리지 않습니다.
4. **호출 낭비를 막는다.** 클라이언트에서 걸러낼 수 있는 오류(빈 입력, 미선택, 비영어)는 **호출 전에** 차단합니다.
5. **자동 재시도는 최대 1회.** 무한 재시도로 비용과 대기 시간이 늘어나는 것을 방지합니다.

### 5.1 예외 처리 명세

#### EX-1. 단어 입력이 비어 있음

| 항목 | 내용 |
|---|---|
| **발생 조건** | 입력창이 완전히 비었거나, 공백·쉼표만 입력됨(`"  "`, `",,,"`, `" , "`) |
| **감지 시점** | 입력 변경 시 실시간 + 생성 버튼 클릭 시 |
| **동작** | 생성 버튼을 **비활성 상태로 유지**. AI 호출 **미발생** |
| **사용자 메시지** | 입력창 하단 인라인: `학습할 단어를 1개 이상 입력해 주세요.` |
| **복구 경로** | 유효한 단어 입력 시 즉시 안내 사라지고 버튼 활성화 |
| **검증** | 빈 문자열 / 공백만 / 쉼표만 / 공백+쉼표 조합 4가지 케이스에서 버튼이 눌리지 않는지 확인 |

#### EX-2. 난이도가 선택되지 않음

| 항목 | 내용 |
|---|---|
| **발생 조건** | 난이도 3지 중 아무것도 선택하지 않은 상태에서 생성 시도 |
| **감지 시점** | 선택 변경 시 실시간 + 생성 버튼 클릭 시 |
| **동작** | 생성 버튼 **비활성**. AI 호출 **미발생**. 기본값을 임의로 채워 넣지 **않음** |
| **사용자 메시지** | 난이도 선택 영역 하단: `난이도를 선택해 주세요.` |
| **복구 경로** | 난이도 선택 시 즉시 해제 |
| **설계 근거** | 기본값을 '보통'으로 미리 채우면 사용자가 자기 수준을 고민하지 않고 넘어가 학습 적합도가 떨어짐. 의도적으로 미선택 상태에서 시작 |
| **검증** | 단어만 입력하고 난이도 미선택 시 버튼이 비활성인지 확인 |

#### EX-3. 입력 단어가 영어가 아님

| 항목 | 내용 |
|---|---|
| **발생 조건** | 한글·숫자·특수문자·이모지가 포함된 단어 (`사과`, `abc123`, `!!!`, `😀`) |
| **감지 시점** | 생성 버튼 클릭 시 (입력 중 실시간 차단은 하지 않음 — 타이핑을 방해하므로) |
| **허용 문자** | 영문자(A~Z, a~z), 하이픈(-), 어퍼스트로피('), 단어 내부 공백(구동사 대응: `look forward to`) |
| **동작 — 일부만 비영어** | 유효한 영어 단어가 1개 이상 남으면 **비영어 항목만 제외하고 진행**. 제외 사실을 사용자에게 고지 |
| **동작 — 전부 비영어** | 호출 **미발생**. 상태 A 유지 |
| **사용자 메시지 (일부)** | `'사과'는 영어가 아니어서 제외했어요. comprehensive로 문제를 만들게요.` |
| **사용자 메시지 (전부)** | `영어 단어만 입력할 수 있어요. 예: comprehensive, comprehensible` |
| **복구 경로** | 입력창의 값은 지우지 않고 그대로 두어 사용자가 수정할 수 있게 함 |
| **검증** | 한글만 / 영한 혼합 / 숫자 포함 / 이모지 / 특수문자 5케이스 |

#### EX-4. 출력된 문항 수가 3개가 아님

| 항목 | 내용 |
|---|---|
| **발생 조건** | AI 응답의 문항 배열 길이가 3이 아님 (0개, 1~2개, 4개 이상) |
| **감지 시점** | 응답 파싱 직후, 화면 렌더 전 |
| **동작 — 0개** | EX-9(응답 실패)와 동일하게 처리. 자동 재시도 1회 |
| **동작 — 1~2개** | **받은 만큼만 진행.** 진행 표시를 실제 개수로 조정(`1 / 2`). 결과 화면 점수도 `n / 2` |
| **동작 — 4개 이상** | **앞에서부터 3개만 사용**하고 나머지 폐기. 사용자에게 알리지 않음(불필요한 혼란) |
| **사용자 메시지 (1~2개)** | 풀이 화면 상단 안내 배너: `이번엔 2문항이 만들어졌어요.` |
| **복구 경로** | 정상 풀이 진행. 결과 화면의 새로 시작으로 재시도 가능 |
| **설계 근거** | 문항 수 부족을 이유로 전체를 버리면 이미 지불한 대기 시간이 낭비됨. 부분 성공을 살리는 것이 사용자 이익에 부합 |
| **검증** | 응답을 인위적으로 0/1/2/5개로 조작해 각 분기 확인 |

#### EX-5. 입력 단어가 미포함된 문항이 출력됨

| 항목 | 내용 |
|---|---|
| **발생 조건** | 문항의 지문·선지 어디에도 입력 단어(또는 그 파생형)가 등장하지 않음 |
| **감지 시점** | 응답 파싱 직후, 문항별 검증 |
| **검증 방법** | 대소문자 무시. **어간 기준 부분 일치**를 허용해야 함 — 어법 유형은 파생형(`comprehensively`, `comprehension`)으로 출제되므로 완전 일치만 요구하면 정상 문항을 오탐함. 입력 단어 앞 4~5글자 어간이 지문 또는 선지 텍스트에 포함되면 통과로 간주 |
| **동작** | 검증 실패 문항만 **폐기**. 남은 문항으로 진행(→ EX-4의 1~2개 분기로 합류). 전부 폐기되면 자동 재시도 1회 → 재실패 시 EX-9 |
| **사용자 메시지** | 폐기 사실 자체는 알리지 않음. 문항 수가 줄면 EX-4 배너로 자연스럽게 전달 |
| **복구 경로** | 새로 시작 |
| **설계 근거** | 입력 단어가 없는 문항은 이 제품의 존재 이유를 부정함. 그냥 보여주면 "일반 토익 문제 생성기"와 구별되지 않음 |
| **검증** | 입력 단어와 무관한 문항을 강제 주입해 폐기되는지 확인 + 파생형 문항이 오탐되지 않는지 확인 |

#### EX-6. 3문항의 유형이 전부 동일함

| 항목 | 내용 |
|---|---|
| **발생 조건** | 3문항의 `type` 값이 모두 같음 (예: 전부 `vocab`) |
| **감지 시점** | 응답 파싱 직후 |
| **동작** | **자동 재생성 1회 시도.** 재시도 결과도 동일 유형이면 **그대로 진행**(추가 호출 없음) |
| **사용자 메시지** | 없음. 재시도 중에는 기존 로딩 표시를 유지 |
| **복구 경로** | 그대로 풀이 진행 |
| **설계 근거** | 유형 혼합은 "뜻만 보고 찍는 습관" 방지가 목적이지만, 이것 때문에 사용자를 무한정 기다리게 하는 것이 더 나쁨. 재시도는 1회로 제한하고 실패 시 학습을 진행시킴 |
| **검증** | 유형이 모두 같은 응답을 주입해 재시도가 1회만 발생하는지 확인 |

#### EX-7. 선지가 4개가 아님

| 항목 | 내용 |
|---|---|
| **발생 조건** | `choices` 배열 길이가 4가 아님 / key가 중복됨 / `answer` 값이 `choices`의 key 중에 없음 / 선지 텍스트가 비어 있음 |
| **감지 시점** | 응답 파싱 직후, 문항별 검증 |
| **동작** | 해당 **문항 폐기**. 선지를 임의로 채워 넣거나 잘라내지 **않음**. 남은 문항으로 진행(→ EX-4로 합류) |
| **사용자 메시지** | 폐기 자체는 미고지. 문항 수 감소는 EX-4 배너로 전달 |
| **복구 경로** | 새로 시작 |
| **설계 근거** | 선지를 임의 보정하면 정답이 2개가 되거나 정답이 사라지는 더 심각한 오류를 만듦. 선지 정합성은 보정 대상이 아니라 폐기 대상 |
| **검증** | 선지 3개 / 5개 / key 중복 / answer 불일치 4케이스 주입 |

#### EX-8. 정답을 선택해도 해설이 표시되지 않음

| 항목 | 내용 |
|---|---|
| **발생 조건** | `explanations`에 선택한 선지의 해설이 없음 / 값이 빈 문자열 / `explanations` 필드 자체가 없음 |
| **감지 시점** | **응답 파싱 직후 (선지 클릭 시점이 아님)** — 4개 선지 전부의 해설 존재 여부를 미리 검증 |
| **동작 — 해설 4개 중 일부 누락** | 해당 **문항 폐기** (→ EX-4로 합류) |
| **동작 — 검증을 통과했으나 렌더 시점에 표시 실패** | 최소 폴백 문구 표시: `이 선지의 해설을 불러오지 못했어요. 정답은 [X]입니다.` **화면은 절대 비워두지 않음** |
| **사용자 메시지** | 위 폴백 문구 |
| **복구 경로** | 다음 문제 버튼은 정상 동작 유지. 해설 실패가 진행을 막지 않아야 함 |
| **설계 근거** | 해설은 이 제품의 핵심 가치이므로 **사후 대응이 아니라 사전 검증**으로 막아야 함. 선지를 클릭한 뒤에야 해설이 없다는 걸 알게 되는 것은 최악의 경험. 다만 예상 못 한 렌더 오류에 대비해 이중 안전장치를 둠 |
| **검증** | 해설 1개 누락 / 빈 문자열 / 필드 통째 누락 3케이스 |

#### EX-9. AI 응답이 실패함

| 항목 | 내용 |
|---|---|
| **발생 조건** | 네트워크 오류 / 인증 오류 / 사용량 한도 초과 / 서버 오류 / 응답이 구조화 데이터가 아님(파싱 불가) / 응답이 비어 있음 |
| **감지 시점** | 호출 응답 수신 직후 |
| **동작** | **자동 재시도 1회** (사용자에게는 계속 로딩으로 보임). 재실패 시 오류 상태 표시 |
| **사용자 메시지 — 일반 실패** | `문제를 만들지 못했어요. 다시 시도해 주세요.` + [다시 시도] 버튼 |
| **사용자 메시지 — 사용량 한도** | `지금은 요청이 많아요. 잠시 후 다시 시도해 주세요.` |
| **사용자 메시지 — 네트워크 끊김** | `인터넷 연결을 확인해 주세요.` |
| **복구 경로** | [다시 시도] = 입력값을 유지한 채 재호출. [입력 수정] = 상태 A로 복귀하되 **입력값 보존** |
| **금지 사항** | 원본 오류 메시지·상태 코드·스택 트레이스를 화면에 노출하지 않음. 자동 재시도는 1회를 넘기지 않음 |
| **검증** | 네트워크 차단 / 잘못된 인증 정보 / 응답 본문을 일반 텍스트로 조작 3케이스 |

#### EX-10. 응답이 오래 걸림

| 항목 | 내용 |
|---|---|
| **발생 조건** | 호출 후 응답이 지연됨 |
| **단계별 동작** | **0~5초:** 기본 로딩 표시 (`3문항을 한 번에 만드는 중`) <br> **5~12초:** 안내 문구 교체 (`조금만 더 기다려 주세요. 거의 다 됐어요.`) — 사용자가 멈춘 것으로 오해하지 않게 함 <br> **12~20초:** 문구 재교체 + **[취소] 버튼 노출** <br> **20초 초과:** 하드 타임아웃. 요청 중단 후 EX-9의 오류 상태로 전환 |
| **사용자 메시지 (타임아웃)** | `시간이 너무 오래 걸려서 중단했어요. 다시 시도해 주세요.` |
| **복구 경로** | [다시 시도] / [입력 수정] (입력값 보존) |
| **금지 사항** | 로딩 중 생성 버튼 재클릭으로 **중복 호출이 발생해서는 안 됨** — 로딩 중 버튼 비활성 필수 |
| **설계 근거** | 5초는 성공 목표이지 실패 기준이 아님. 5초에 요청을 끊으면 정상 응답까지 버리게 되므로, 5초는 문구 전환 시점으로만 사용 |
| **검증** | 응답을 인위적으로 6초 / 15초 / 25초 지연시켜 각 단계 확인 + 로딩 중 버튼 연타 |

#### EX-11. 공유 기능이 동작하지 않는 환경

| 항목 | 내용 |
|---|---|
| **발생 조건** | 시스템 공유 시트를 지원하지 않는 환경(주로 데스크톱 브라우저) / 사용자가 공유 시트를 취소함 / 클립보드 접근이 차단됨 |
| **감지 시점** | 공유 버튼 클릭 시 |
| **동작 — 공유 미지원** | 자동으로 **클립보드 복사로 대체**. 사용자에게는 실패로 보이지 않아야 함 |
| **동작 — 사용자 취소** | 아무 동작도 하지 않음. **오류 메시지를 띄우지 않음** (취소는 오류가 아님) |
| **동작 — 클립보드도 차단** | 공유 텍스트를 **선택 가능한 텍스트 영역으로 화면에 표시**해 수동 복사 가능하게 함 |
| **사용자 메시지** | 복사 성공 시: `복사했어요!` 토스트 / 수동 복사 폴백 시: `아래 내용을 직접 복사해 주세요.` |
| **복구 경로** | 결과 화면 유지. 다른 버튼 정상 동작 |
| **검증** | 데스크톱 브라우저 / 공유 시트 취소 / 클립보드 권한 거부 3케이스 |

#### EX-12. 입력 단어가 6개 이상

| 항목 | 내용 |
|---|---|
| **발생 조건** | 쉼표 분리 결과가 6개 이상 |
| **감지 시점** | 입력 변경 시 실시간 |
| **동작** | **앞에서부터 5개만 사용**하고 나머지는 무시. 호출은 정상 진행 |
| **사용자 메시지** | `한 번에 5개까지만 사용해요. 앞의 5개로 만들게요.` |
| **복구 경로** | 사용자가 직접 입력을 줄일 수 있음 |
| **설계 근거** | 단어가 많을수록 3문항에 골고루 반영되지 못해 타깃 정확도가 떨어짐. 차단이 아니라 절삭 + 고지로 처리해 흐름을 끊지 않음 |
| **검증** | 7개 입력 시 앞 5개만 반영되는지 확인 |

### 5.2 예외 우선순위 (동시 발생 시)

여러 예외가 겹칠 때의 처리 순서입니다.

```
1순위 (호출 전 차단)   EX-1 빈 입력 → EX-2 난이도 미선택 → EX-3 비영어 → EX-12 개수 초과
2순위 (응답 전체 판정) EX-9 응답 실패 → EX-10 타임아웃
3순위 (문항별 검증)    EX-7 선지 불량 → EX-8 해설 누락 → EX-5 단어 미포함
4순위 (세트 판정)      EX-4 문항 수 → EX-6 유형 편중
5순위 (표시 시점)      EX-11 공유 실패
```

**자동 재시도가 허용되는 예외는 EX-4(0개) / EX-5(전량 폐기) / EX-6 / EX-9 네 가지뿐이며, 각각 최대 1회입니다.** 한 세션에서 재시도는 총 1회를 넘기지 않습니다(예: EX-9로 재시도한 뒤 EX-6이 발생해도 추가 재시도하지 않음).

---

## 6) 완료 조건

### 6.1 기능 완료 체크리스트

| # | 항목 | 판정 |
|---|---|---|
| D-1 | 화면이 1개이며, 입력·풀이·결과가 페이지 이동 없이 상태 전환으로만 동작한다 | 브라우저 뒤로가기가 화면을 바꾸지 않음 |
| D-2 | AI 호출이 세션당 1회만 발생한다 | 풀이·결과 단계에서 네트워크 요청 0건 |
| D-3 | 파트 선택 UI가 화면에 존재하지 않는다 | 시각 확인 |
| D-4 | 난이도 선택이 동작하고 결과에 반영된다 | 쉬움/어려움 각 3회 생성 후 체감 차이 확인 |
| D-5 | 3문항이 생성되고, 유형이 전부 동일하지 않다 | 20회 생성 중 전부 동일 = 0회 (S6) |
| D-6 | 각 문항의 선지가 4개이며 정답이 유일하다 | 60문항 수기 확인 (S5) |
| D-7 | 선지 선택 시 200ms 이내에 해설 4블록이 표시된다 | 네트워크 탭 + 체감 확인 (S2) |
| D-8 | 오답 선택 시 그 선지의 오답 이유가 표시된다 | 각 문항에서 오답 3개 모두 클릭 확인 (US-6) |
| D-9 | 결과 화면에 점수·유형별 정오·틀린 단어가 표시된다 | 시각 확인 |
| D-10 | 복사·공유·새로 시작이 동작한다 | 3개 버튼 각각 확인 (EX-11 포함) |
| D-11 | 새로고침 시 모든 데이터가 사라진다 | 저장소 미사용 확인 |
| D-12 | 어떤 형태의 저장소도 사용하지 않는다 | 코드 검색으로 저장소 API 호출 0건 확인 |

### 6.2 예외 처리 완료 체크리스트

**EX-1 ~ EX-12 전 항목이 각자의 "검증" 칸에 적힌 케이스를 통과해야 합니다.** 총 검증 케이스는 다음과 같습니다.

| 예외 | 케이스 수 | 통과 기준 |
|---|---|---|
| EX-1 빈 입력 | 4 | 4/4 버튼 비활성 |
| EX-2 난이도 미선택 | 1 | 버튼 비활성 |
| EX-3 비영어 | 5 | 5/5 안내 표시 + 호출 미발생(전부 비영어 시) |
| EX-4 문항 수 이상 | 4 | 0/1/2/5개 각 분기 정상 |
| EX-5 단어 미포함 | 2 | 미포함 폐기 + 파생형 오탐 없음 |
| EX-6 유형 편중 | 1 | 재시도 정확히 1회 |
| EX-7 선지 불량 | 4 | 4/4 문항 폐기 |
| EX-8 해설 누락 | 3 | 3/3 사전 폐기, 화면 공백 없음 |
| EX-9 응답 실패 | 3 | 3/3 복구 버튼 동작, 원본 오류 미노출 |
| EX-10 지연 | 4 | 6/15/25초 단계 전환 + 버튼 연타 시 중복 호출 0건 |
| EX-11 공유 실패 | 3 | 3/3 폴백 동작, 취소 시 오류 미표시 |
| EX-12 개수 초과 | 1 | 앞 5개만 반영 + 고지 |
| **합계** | **35** | **35/35 통과** |

### 6.3 성능·품질 완료 조건

§1.2의 **S1~S6을 20회 연속 생성 테스트로 측정하여 전부 목표값을 충족**해야 합니다. S7·S8은 사용자 5명 테스트로 확인합니다.

### 6.4 미달 시 대응 규칙

목표를 달성하지 못했을 때 **기능을 추가해서 해결하지 않습니다.** 아래 순서로 범위를 줄입니다.

1. S1(속도) 미달 → 문항 수를 3개에서 2개로 축소
2. 그래도 미달 → 유형 혼합 지시문을 단순화(유형 2종으로 축소)
3. 시간 부족 → 공유 기능을 클립보드 복사 전용으로 단순화
4. 그래도 부족 → 난이도 선택을 '보통' 고정으로 축소

**단, 어떤 경우에도 §5의 예외 처리 EX-1 ~ EX-10은 축소 대상이 아닙니다.** 예외 처리가 빠진 상태로는 완료로 판정하지 않습니다.